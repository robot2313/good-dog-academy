import type { LessonDefinition } from '../../../domain/models';

// Production lesson content is intentionally deferred. Milestone 5 establishes
// the immutable catalogue architecture without shipping final lesson copy.
export const bundledLessonDefinitions: readonly LessonDefinition[] = Object.freeze([]);
